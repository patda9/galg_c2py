# C to Python conversion Genetic Algorithm
## Usage
Normal enviroment setup:

  `pip install numpy`

With virtual environment (Optional):

  `python -m venv .`

  `Script/Activate.ps1 (Windows powershell)`
  
  `pip install numpy`
